name: Release

on:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest

    # Only run when the HEAD commit was authored/committed by the GitHub Actions bot.
    # (Depending on how the push was created, github.actor can be misleading.)
    if: >-
      github.event.head_commit.author.username == 'github-actions[bot]' ||
      github.event.head_commit.author.name == 'github-actions[bot]' ||
      github.event.head_commit.committer.username == 'github-actions[bot]' ||
      github.event.head_commit.committer.name == 'github-actions[bot]' ||
      github.event.pusher.name == 'github-actions[bot]'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install prerequisites
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          Install-Module Pester -Force -Scope CurrentUser -MinimumVersion 5.0.0

      - name: Run tests
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          Invoke-Pester -Path "./GitSplit.Tests.ps1" -CI -Output Detailed

      - name: Read version from manifest
        id: version
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'

          $moduleName = 'GitSplit'
          $manifestPath = Join-Path $PWD "$moduleName.psd1"
          if (-not (Test-Path $manifestPath)) { throw "Manifest not found at $manifestPath" }

          $manifest = Import-PowerShellDataFile -Path $manifestPath
          $v = [string]$manifest.ModuleVersion
          if (-not $v) { throw 'ModuleVersion not found in manifest.' }

          "version=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          body: |
            Automated release for version v${{ steps.version.outputs.version }}.
            
            Changes:
            - See commit history for details.
          generate_release_notes: true
